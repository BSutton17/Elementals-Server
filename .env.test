# Loaded by `npm test` via --env-file. Committed on purpose: it holds no
# secrets, only the switch that arms the guard in db/client.ts so a test can
# never reach the real database. See ALLOW_TEST_DB there to opt back in.
NODE_ENV=test
